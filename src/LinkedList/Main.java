package LinkedList;
// we use linkedlist instead arrays bcoz of these limitations
// fixed size
//continuous memory allocation
//hard to add/delete the element in the middle

//even linkedlist have drawbacks like inorder to access the middle element we have to go all the way


import java.util.LinkedList;

public class Main {
    public static void main(String[] args) {
        LinkedList<Integer> list = new LinkedList<>();
        list.add(34);
        list.add(34);
        list.add(34);
        list.add(34);
        list.add(34);
        list.remove();


    }
}
